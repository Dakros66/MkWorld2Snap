# Optional local overrides for MkWorld2Snap.
# Copy to .env only when you need to change defaults.

MAX_UPLOAD_MB=500
CLEANUP_TEMP_AFTER_SECONDS=300
RETAIN_FAILED_FILES=false

# Used by Docker/web mode for generated metadata endpoints.
DOMAINHOST=localhost
